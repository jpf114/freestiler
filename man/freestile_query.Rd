% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freestile.R
\name{freestile_query}
\alias{freestile_query}
\title{Create vector tiles from a DuckDB SQL query}
\usage{
freestile_query(
  query,
  output,
  db_path = NULL,
  layer_name = NULL,
  tile_format = "mlt",
  min_zoom = 0L,
  max_zoom = 14L,
  base_zoom = NULL,
  drop_rate = NULL,
  cluster_distance = NULL,
  cluster_maxzoom = NULL,
  coalesce = FALSE,
  simplification = TRUE,
  overwrite = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{query}{Character. A SQL query that returns a geometry column. DuckDB
spatial functions like \code{ST_Read()} and \code{read_parquet()} are available.}

\item{output}{Character. Path for the output .pmtiles file.}

\item{db_path}{Character. Path to a DuckDB database file, or NULL (default)
for an in-memory database.}

\item{layer_name}{Character. Name for the tile layer. If NULL, derived from
the output filename.}

\item{tile_format}{Character. \code{"mlt"} (default) or \code{"mvt"}.}

\item{min_zoom}{Integer. Minimum zoom level (default 0).}

\item{max_zoom}{Integer. Maximum zoom level (default 14).}

\item{base_zoom}{Integer. Zoom level at and above which all features are
present. NULL (default) uses max_zoom.}

\item{drop_rate}{Numeric. Exponential drop rate. NULL (default) disables.}

\item{cluster_distance}{Numeric. Pixel distance for clustering. NULL disables.}

\item{cluster_maxzoom}{Integer. Max zoom for clustering. Default max_zoom - 1.}

\item{coalesce}{Logical. Whether to merge features with identical attributes
(default FALSE).}

\item{simplification}{Logical. Whether to snap geometries to the tile pixel
grid (default TRUE).}

\item{overwrite}{Logical. Whether to overwrite existing output (default TRUE).}

\item{quiet}{Logical. Whether to suppress progress (default FALSE).}
}
\value{
The output file path (invisibly).
}
\description{
Executes a SQL query via DuckDB's spatial extension and pipes the results
directly into the Rust tiling engine. Data never touches R memory, making
this ideal for large datasets. Requires freestiler to be compiled with
\code{FREESTILER_DUCKDB=true}.
}
\examples{
\dontrun{
# Query a GeoParquet file
freestile_query(
  "SELECT * FROM read_parquet('data.parquet') WHERE pop > 50000",
  "output.pmtiles"
)

# Query a Shapefile
freestile_query(
  "SELECT * FROM ST_Read('counties.shp')",
  "counties.pmtiles"
)

# Query with an existing DuckDB database
freestile_query(
  "SELECT * FROM my_table WHERE region = 'West'",
  "west.pmtiles",
  db_path = "my_database.duckdb"
)
}

}
