# freestiler 项目逻辑设计与业务图谱

本文档用于完整说明当前项目的设计逻辑、核心结构，以及 `PostGIS 输入 / MongoDB 输出` 定制化功能的业务实现方式。

---

## 1. 项目概述

### 1.1 项目定位

`freestiler` 是一个以 Rust 为核心引擎的矢量切片项目。  
它的本质不是单一的“文件转切片工具”，而是一个“统一地理数据输入层 + 切片配置层 + 切片编码层 + 多输出层”的架构。

当前项目同时包含两类能力：

- 原生主线能力
  - GeoPandas 输入
  - 文件输入
  - DuckDB / SQL 输入
  - PMTiles 输出
  - `MLT / MVT` 两种切片编码格式
- 定制化能力
  - `PostGIS` 输入
  - `MongoDB` 输出
  - 大表流式切片处理
  - 固定业务文档结构 `id / x / y / z / data`
  - 针对 Mongo 运行特性的 profile 预设

### 1.2 项目总览图

```mermaid
flowchart TB
    A["freestiler\nRust 矢量切片引擎"] --> B["原生能力"]
    A --> C["定制化能力"]

    B --> B1["GeoPandas / File / DuckDB / Query 输入"]
    B --> B2["PMTiles 输出"]
    B --> B3["MLT / MVT 编码"]
    B --> B4["多图层 / 聚合 / 简化 / 聚类"]

    C --> C1["PostGIS 输入"]
    C --> C2["MongoDB 输出"]
    C --> C3["Streaming 流式处理"]
    C --> C4["Mongo 业务字段固定"]
    C --> C5["Mongo profile 预设"]

    style A fill:#0f172a,color:#fff,stroke:#0f172a,stroke-width:2px
    style B fill:#1d4ed8,color:#fff,stroke:#1d4ed8,stroke-width:2px
    style C fill:#7c2d12,color:#fff,stroke:#7c2d12,stroke-width:2px
```

### 1.3 当前项目最重要的结论

- 该项目的统一核心是 Rust 切片引擎，不同输入输出只是围绕这个核心做适配。
- 当前交付重点是 `PostGIS -> MongoDB` 这条定制链路。
- 这条链路已经从“可用”演进到“有参数边界、有默认策略、有真实验证结果”的状态。

---

## 2. 总体架构设计

### 2.1 分层架构图

```mermaid
flowchart LR
    U["用户入口"] --> U1["Python API"]
    U["用户入口"] --> U2["CLI"]

    U1 --> P1["python/python/freestiler/__init__.py"]
    U2 --> P2["python/src/bin/freestiler-postgis-mongo.rs"]

    P1 --> R1["python/src/lib.rs\nPyO3 绑定层"]
    P2 --> R2["直接调用 freestiler-core"]

    R1 --> CORE["src/rust/freestiler-core\n核心引擎"]
    R2 --> CORE

    CORE --> IN["输入层"]
    CORE --> CFG["配置层"]
    CORE --> TILER["切片生成层"]
    CORE --> OUT["输出层"]

    IN --> IN1["GeoPandas"]
    IN --> IN2["文件"]
    IN --> IN3["DuckDB"]
    IN --> IN4["PostGIS"]

    OUT --> OUT1["PMTiles"]
    OUT --> OUT2["MongoDB"]

    style CORE fill:#0f172a,color:#fff,stroke:#0f172a,stroke-width:2px
    style OUT2 fill:#14532d,color:#fff,stroke:#14532d,stroke-width:2px
```

### 2.2 各层职责

```mermaid
flowchart TB
    A["入口层"] --> A1["Python 包装 API"]
    A --> A2["CLI 参数解析"]

    B["绑定层"] --> B1["参数透传"]
    B --> B2["运行模式选择"]
    B --> B3["Mongo profile 应用"]

    C["输入层"] --> C1["Geo 数据预处理"]
    C --> C2["PostGIS 读取"]
    C --> C3["DuckDB 读取"]

    D["配置层"] --> D1["TileConfig 构建"]
    D --> D2["zoom 区间控制"]
    D --> D3["编码格式控制"]

    E["切片层"] --> E1["分区"]
    E --> E2["特征分发"]
    E --> E3["MVT / MLT 编码"]

    F["输出层"] --> F1["PMTiles Writer"]
    F --> F2["Mongo Writer"]
    F --> F3["Mongo Streaming Sink"]

    A --> B --> D --> E --> F
    C --> E
```

### 2.3 关键文件位置

- Python 入口：[python/python/freestiler/__init__.py](/D:/Code/MyProject/freestiler/python/python/freestiler/__init__.py)
- Python 绑定：[python/src/lib.rs](/D:/Code/MyProject/freestiler/python/src/lib.rs)
- CLI 入口：[python/src/bin/freestiler-postgis-mongo.rs](/D:/Code/MyProject/freestiler/python/src/bin/freestiler-postgis-mongo.rs)
- 核心引擎：[src/rust/freestiler-core/src/engine.rs](/D:/Code/MyProject/freestiler/src/rust/freestiler-core/src/engine.rs)
- Mongo sink：[src/rust/freestiler-core/src/sink/mongo.rs](/D:/Code/MyProject/freestiler/src/rust/freestiler-core/src/sink/mongo.rs)
- Mongo writer：[src/rust/freestiler-core/src/mongo_writer.rs](/D:/Code/MyProject/freestiler/src/rust/freestiler-core/src/mongo_writer.rs)

---

## 3. 主项目标准处理逻辑

### 3.1 标准数据流

```mermaid
flowchart TB
    A["输入数据"] --> B{"输入类型"}
    B --> B1["GeoPandas"]
    B --> B2["文件"]
    B --> B3["DuckDB Query"]
    B --> B4["PostGIS Query"]

    B1 --> C["统一 Feature / Layer 数据结构"]
    B2 --> C
    B3 --> C
    B4 --> C

    C --> D["TileConfig\nmin_zoom / max_zoom / format / simplify / cluster / coalesce"]
    D --> E["按 zoom / tile 组织特征"]
    E --> F{"切片编码"}
    F --> F1["MLT"]
    F --> F2["MVT"]

    F1 --> G{"输出介质"}
    F2 --> G

    G --> G1["PMTiles"]
    G --> G2["MongoDB"]
```

### 3.2 原生主线与定制主线对比

```mermaid
flowchart LR
    A["原生主线"] --> A1["Geo / File / DuckDB"]
    A1 --> A2["Rust 切片引擎"]
    A2 --> A3["PMTiles"]

    B["定制主线"] --> B1["PostGIS"]
    B1 --> B2["Rust 切片引擎"]
    B2 --> B3["MongoDB"]

    A2 -. 共享同一核心 .- B2

    style B fill:#7c2d12,color:#fff,stroke:#7c2d12,stroke-width:2px
    style B3 fill:#14532d,color:#fff,stroke:#14532d,stroke-width:2px
```

---

## 4. 定制化功能：PostGIS 输入 / Mongo 输出

### 4.1 定制化目标

这部分改造的目标不是简单增加一个数据库输出，而是建立一条适用于大地理表的稳定切片处理链路：

- 从 `PostGIS` 直接读取空间数据
- 尽量避免整表先装入内存
- 在切片阶段保留 `MVT` 编码兼容性
- 输出到 `MongoDB`
- 固定业务文档结构，便于下游系统直接消费

### 4.2 定制化业务总图

```mermaid
flowchart TB
    A["PostGIS SQL / 大表"] --> B["空间分区读取"]
    B --> C["特征归一化"]
    C --> D["按 tile 编码 MVT"]
    D --> E["MongoDB 写入"]
    E --> F["上层系统按 z/x/y 读取 data"]

    G["业务约束"] --> G1["内存受控"]
    G --> G2["支持大表"]
    G --> G3["规避低 zoom 超大 tile 风险"]
    G --> G4["保持结果结构稳定"]
```

### 4.3 定制链路详细处理流程

```mermaid
flowchart TB
    A["输入参数\npostgis / sql / mongo / mongo_profile / batch_size / mongo_batch_size"] --> B["解析连接串"]
    B --> C["构造 PostgisConfig"]
    C --> D["应用 Mongo profile"]
    D --> E["生成 TileConfig"]
    E --> F["输出运行时 advisory"]

    F --> G{"streaming = true ?"}
    G -->|是| H["run_postgis_to_mongo_stream"]
    G -->|否| I["probe_and_maybe_load_layers"]

    I --> J{"是否大结果集"}
    J -->|否| K["内存装载 -> 分段写 Mongo"]
    J -->|是| L["by-zoom 路径写 Mongo"]

    H --> M["按 partition_zoom = max_zoom 划分分区"]
    M --> N["逐分区读取 PostGIS"]
    N --> O["逐 tile 编码 MVT"]
    O --> P["MongoSink 批量 flush"]

    K --> Q["generate_tiles_to_target"]
    L --> R["generate_postgis_query_to_mongo_by_zoom"]

    P --> S["Mongo Collection"]
    Q --> S
    R --> S

    style H fill:#7c2d12,color:#fff,stroke:#7c2d12,stroke-width:2px
    style S fill:#14532d,color:#fff,stroke:#14532d,stroke-width:2px
```

### 4.4 Mongo 文档结构图

```mermaid
classDiagram
    class TileDocument {
        +string id
        +int z
        +int x
        +int y
        +binary data
    }
```

字段语义：

- `id`：业务主键，逻辑上对应 tile 唯一标识
- `z`：缩放层级
- `x`：tile 列号
- `y`：tile 行号
- `data`：切片二进制内容

### 4.5 上层业务使用视角

```mermaid
sequenceDiagram
    participant PG as PostGIS
    participant FT as freestiler
    participant EN as MVT Encoder
    participant MG as MongoDB
    participant APP as 上层应用

    PG->>FT: SQL + geometry + 属性
    FT->>FT: 分区 / 组织 / 切片
    FT->>EN: 编码为 MVT
    EN->>MG: 写入 id/x/y/z/data
    APP->>MG: 按 z/x/y 查询
    MG-->>APP: 返回 data
```

---

## 5. 流式架构设计

### 5.1 为什么必须支持流式

`PostGIS -> MongoDB` 这条链路面对的是大表。  
如果仍然沿用“小数据先全量装载，再统一切片”的方式，会遇到三个问题：

- 单次内存占用过高
- 大表处理不稳定
- 低层级 tile 聚合过大时，Mongo 单文档存在边界风险

所以这里真正合理的结构是：

- 先按空间分区读取
- 边读边切片
- 边切片边写 Mongo

### 5.2 流式核心节点图

```mermaid
flowchart TB
    A["PostGIS cursor batch read"] --> B["partition reader"]
    B --> C["feature normalize"]
    C --> D["tile dispatch"]
    D --> E["MVT encode"]
    E --> F["pending tile buffer"]
    F --> G{"达到 mongo_batch_size ?"}
    G -->|是| H["flush to Mongo"]
    G -->|否| I["继续累计"]
    H --> J["collection write"]
    I --> B
```

### 5.3 运行模式选择图

```mermaid
flowchart LR
    A["PostGIS -> Mongo 请求"] --> B{"streaming=true ?"}
    B -->|是| C["直接流式路径"]
    B -->|否| D["先 probe"]
    D --> E{"是否小结果集"}
    E -->|是| F["内存路径"]
    E -->|否| G["large-data by-zoom 路径"]
```

### 5.4 结构判断

从原生架构角度看，这条定制链路适合流式处理。  
真正的硬边界并不在“流式本身”，而在：

- 低 zoom 时单 tile 聚合密度太高
- Mongo 单文档存在 16MB 限制

也就是说，流式能解决“处理过程”的问题，但不能消除“结果载体边界”的问题。

---

## 6. MLT / MVT 与当前定制方案的关系

### 6.1 编码选择图

```mermaid
flowchart LR
    A["切片编码选择"] --> B["MLT"]
    A --> C["MVT"]

    B --> B1["前沿格式"]
    B --> B2["适合主项目原生探索"]

    C --> C1["兼容性强"]
    C --> C2["更适合当前 Mongo 链路"]
```

### 6.2 为什么当前 Mongo 主线选择 MVT

对于当前这条定制化业务链路，`MVT` 更合理，原因有三点：

- 与现有读取消费生态兼容性更强
- 当前真实验证和 profile 预设都围绕 `MVT` 固化
- Mongo 输出链路首先追求“稳定可消费”，不是优先追求前沿格式实验

### 6.3 MLT 在当前流式链路中的位置

`MLT` 不是没价值，而是它当前不是这条 Mongo 业务主线的最优默认选择。

更准确地说：

- 主项目保留 `MLT`，说明架构本身对前沿切片格式是开放的
- 但当前定制链路为了生产可落地，默认收敛到 `MVT`

---

## 7. Mongo Profile 与运行边界

### 7.1 Profile 图

```mermaid
flowchart LR
    A["Mongo Profile"] --> B["recommended"]
    A --> C["safe"]
    A --> D["high_detail"]

    B --> B1["MVT"]
    B --> B2["zoom 10..12"]
    B --> B3["默认生产"]

    C --> C1["MVT"]
    C --> C2["zoom 6..12"]
    C --> C3["保守覆盖"]

    D --> D1["MVT"]
    D --> D2["zoom 14..15"]
    D --> D3["高精度高成本"]
```

### 7.2 风险边界图

```mermaid
flowchart TB
    A["低 zoom"] --> A1["单 tile 聚合密度高"]
    A1 --> A2["Mongo 文档可能过大"]
    A2 --> A3["z5 及以下不建议"]

    B["中高 zoom"] --> B1["10..12 最均衡"]
    B1 --> B2["推荐默认区间"]

    C["高 zoom"] --> C1["14..15 可用"]
    C1 --> C2["tile 数暴涨"]
    C2 --> C3["适合按需高精度"]
```

### 7.3 当前建议参数

```mermaid
mindmap
  root((当前建议))
    Profile
      recommended
      MVT
      zoom 10..12
    Throughput
      batch_size 20000
      mongo_batch_size 8192
    Runtime
      streaming true
      create_indexes true
```

### 7.4 当前边界结论

- 默认生产 profile：`recommended`
- 默认区间：`10..12`
- Mongo 安全下限：`min_zoom >= 6`
- `14..15` 可作为高精度区间
- `z5` 及以下不建议落 Mongo 文档方案

---

## 8. 当前项目现状

### 8.1 当前真实实现状态

- 已具备 `PostGIS -> MongoDB` 流式链路
- Mongo 文档结构已固定为 `id / x / y / z / data`
- Python API 与 CLI 都已支持该链路
- `CLI vs Python API` 已做真实链路一致性验证
- `recommended / safe / high_detail` 三组 profile 已完成验证
- 默认吞吐参数已固定为：
  - `batch_size = 20000`
  - `mongo_batch_size = 8192`

### 8.2 当前最重要的项目结论图

```mermaid
flowchart TB
    A["当前核心结论"] --> B["默认 profile = recommended"]
    A --> C["默认吞吐 = 20000 / 8192"]
    A --> D["默认编码 = MVT"]
    A --> E["推荐区间 = zoom 10..12"]
    A --> F["Mongo 安全下限 = min_zoom >= 6"]
    A --> G["z5 及以下不建议"]
```

### 8.3 适合汇报时直接使用的结论

- 主项目是一个统一矢量切片架构，而不是单一格式转换工具。
- 当前定制化功能的核心，是把 `PostGIS -> MongoDB` 做成真正适合大表的稳定链路。
- 这条链路的关键不是“能否写 Mongo”，而是“如何在大表下稳定处理，同时规避低 zoom 大 tile 风险”。
- 因此当前方案最终收敛为：
  - `recommended profile`
  - `MVT`
  - `streaming=true`
  - `batch_size=20000`
  - `mongo_batch_size=8192`
  - 默认生产区间 `10..12`

